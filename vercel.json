{
  "installCommand": "npm install",
  "buildCommand": "cd frontend && npm install --no-optional && npx vite build",
  "outputDirectory": "frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
